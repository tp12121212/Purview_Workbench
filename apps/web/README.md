# Web (Phase 1)

React + Vite shell for delegated sign-in and tenant onboarding.

## Local run

```bash
pnpm --filter @purview/web dev
```

## Required env vars

- `VITE_ENTRA_CLIENT_ID`
- `VITE_ENTRA_AUTHORITY`
- `VITE_ENTRA_REDIRECT_URI`
- `VITE_API_AUDIENCE`
- `VITE_API_BASE_URL`
